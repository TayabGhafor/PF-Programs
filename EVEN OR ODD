#include<stdio.h>
int main()
{
    int n=0;

    printf("Enter an integer = ");
    scanf("%d", &n);

    if (n%2==0)
        {
            printf("The number %d is even", n);
        }
    else
    {
        printf("The number %d is odd", n);
    }
    return 0;
}
