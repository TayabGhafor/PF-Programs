#include<stdio.h>
int main ()
{
    int num;

    while (num<=10)
    {
    printf("Enter 10 integers = ");
    scanf("%d", &num);
    }
    if (num>=0)
    {
        printf("%d are positive", num);
    }
    else
    {
        printf("%d are negative", num);
    }

    return 0;
}
